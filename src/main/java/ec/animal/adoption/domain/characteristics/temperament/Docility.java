package ec.animal.adoption.domain.characteristics.temperament;

public enum Docility implements Temperament {
    EXTREMELY_DOCILE, DOCILE, NEITHER_DOCILE_NOR_DOMINANT, DOMINANT, EXTREMELY_DOMINANT;
}
