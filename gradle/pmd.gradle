pmd {
    toolVersion = "6.21.0"
    ruleSets = []
    ruleSetConfig = resources.text.fromFile("pmd/ruleset.xml")
}

tasks.withType(Pmd) {
    reports {
        xml.required = true
        html.required = true
    }
}