sourceSets {
    main {
        resources {
            srcDir file('src/main/resources')
        }
    }

    integrationTest {
        java {
            srcDir file('src/integration-test/java')
            compileClasspath += main.output + test.output
            runtimeClasspath += main.output + test.output
        }
        resources {
            srcDir file('src/integration-test/resources')
        }
    }

    apiTest {
        java {
            srcDir file('src/api-test/java')
            compileClasspath += main.output + test.output
            runtimeClasspath += main.output + test.output
        }
        resources {
            srcDir file('src/api-test/resources')
        }
    }

    archUnitTest {
        java {
            srcDir file('src/archunit-test/java')
            compileClasspath += main.output + test.output
            runtimeClasspath += main.output + test.output
        }
        resources {
            srcDir file('src/archunit-test/resources')
        }
    }
}